class Solution(object):
    def containsDuplicate(self, nums):
        """
        :type nums: List[int]
        :rtype: bool
        """
        freq = set();
        for o in nums:
            if o in freq
                return True;
            freq.add(o);
        return False;
        
